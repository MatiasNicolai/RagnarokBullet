// Logical playfield size (world units == unscaled pixels). Vertical danmaku
// aspect; the renderer letterboxes this into the window.
export const FIELD_W = 600;
export const FIELD_H = 800;
export const TICK_RATE = 60;
